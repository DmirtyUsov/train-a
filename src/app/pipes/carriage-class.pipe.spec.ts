import { CarriageClassPipe } from './carriage-class.pipe';

describe('CarriageClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CarriageClassPipe();
    expect(pipe).toBeTruthy();
  });
});
